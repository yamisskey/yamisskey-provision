version: '3.4'

services:
  element:
    image: vectorim/element-web:latest
    container_name: element
    restart: always
    volumes:
      - ./element-config/config.element.yami.ski.json:/app/config.element.yami.ski.json
    ports:
      - "8081:80"